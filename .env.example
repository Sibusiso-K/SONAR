# Copy to .env for local testing. Never commit the real file — .env is
# already gitignored. For the actual autonomous pipeline (GitHub Actions),
# these same names must also be added as repository secrets:
# Settings -> Secrets and variables -> Actions -> New repository secret.
# No tool available to this session can set those for you — it's a
# manual, dashboard-only step by design (secrets management isn't exposed
# over the GitHub MCP API).

# --- Supabase (scripts/sonar_db.py) ---
SUPABASE_URL=https://txmxygjqndenkcdpweym.supabase.co
SUPABASE_SERVICE_KEY=

# --- AI providers (verification/extraction pipeline, not yet built) ---
# Groq: fast inference, generous free tier, refills daily.
GROQ_API_KEY=
# AIsa.one: agent gateway - model routing + X/Twitter, Tavily, Perplexity,
# YouTube SERP lookups. See docs/AUTONOMY.md "Model routing".
AISA_API_KEY=
